1.把所有世界的传送都录制一边
2，把所有NPC的瞬移都录制一边
3，编写指定地图指定坐标的瞬移api
4.编写指定地图指定坐标的飞行api 飞行到指定地图的指定坐标



使用旧的任务链接传送 0x00B5   组合各地图盘龙图腾的访问过程组合出瞬移到各个地图的api接口并封装至图形界面。

录制中的 0x0042 是周期帧


校验算法已经从原客户端代码里定位并用实录验算通过：首次 counter=0 的包用 RC4 解出种子，之后每次执行“加常量再循环右移 3 位”，20 组应答值全部一致。另一个重要结论是：客户端不是自己每 30 秒发心跳，而是服务器每 30 秒下发挑战，客户端立即回包；我会按这个服务器驱动模型实现，避免长时间运行后节拍漂移。



跑环需要合各种npc对话，现在有了传送到各个地图的接口了，跑环任务自动化可以实现了吗？


飞行到指定坐标的能力录制

思考一下当前录制和整理出来的能力如何组合出 瞬移到指定地图指定坐标的能力